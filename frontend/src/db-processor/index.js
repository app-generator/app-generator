import React, { useState } from 'react';
import Select from 'react-select';

const DBProcessor = () => {

    return (
        <div className="min-h-screen bg-gray-100 p-6">
            <h1>DB Processor: wip</h1>
        </div>
    );
};

export default DBProcessor;
