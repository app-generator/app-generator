import React, { useState } from 'react';
import Select from 'react-select';

const DBMigrator = () => {

    return (
        <div className="min-h-screen bg-gray-100 p-6">
            <h1>DB Migrator: wip</h1>
        </div>
    );
};

export default DBMigrator;
